services:
  - type: web
    name: biblioteca-virtual
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /
    autoDeploy: true
